import { Story, Meta } from '@storybook/react'
import CardSmallSize from '.'

export default {
  title: 'CardSmallSize',
  component: CardSmallSize
} as Meta

export const Default: Story = () => <CardSmallSize />

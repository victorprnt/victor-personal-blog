import { Story, Meta } from '@storybook/react'
import FirstCardSection from '.'

export default {
  title: 'FirstCardSection',
  component: FirstCardSection
} as Meta

export const Default: Story = () => <FirstCardSection />

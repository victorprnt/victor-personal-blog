import { Story, Meta } from '@storybook/react'
import MainBanner from '.'

export default {
  title: 'MainBanner',
  component: MainBanner
} as Meta

export const Default: Story = () => <MainBanner />

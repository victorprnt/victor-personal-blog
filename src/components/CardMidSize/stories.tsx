import { Story, Meta } from '@storybook/react'
import CardMidSize from '.'

export default {
  title: 'CardMidSize',
  component: CardMidSize
} as Meta

export const Default: Story = () => <CardMidSize />
